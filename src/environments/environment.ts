// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  API_URL:  'http://localhost:3000/api/v1',
  API_AUTH: 'http://localhost:3000/api/v1/auth',
  X_ACCESS_TOKEN_KEY_HEADER: 'X-ACCESS-TOKEN',
  X_ACCESS_HEADER: 'X-ACCESS',
  X_ACCESS_TOKEN_PASSWORD: "79710619e2c6e75105fb3c84b8b35929cd06a346b2df00156b28ca20454cf19f",
  X_ACCESS_TOKEN_KEY: "f671b539a904aa84a59fa8f6c0931cc13d2f101961d43efe066abf376a04f321",
};
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
